#include "main.h"

/**
 * mul-function check if letter is upper case or not
 * @a:paramter
 * @b:paramter
 * Return: return 1 if upper case and 0 if not
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;
	return (mul);
}
