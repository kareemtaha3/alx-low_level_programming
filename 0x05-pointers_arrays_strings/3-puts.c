#include "main.h"
/**
 * _puts-function to print a string
 * @str:string
 * Return: no return value
 */

void _puts(char *str)
{
	int count, i;

	i = 0, count = 0;
	while (s[i] != '\0')
	{
		_putchar(s[i]);
		++i;
	}
	_putchar('\n');
}
